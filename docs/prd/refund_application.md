# Формирование заявления на возврат

Автоматическое создание заявления на возврат в PDF по данным JSON согласно
`ТЗ_формирование_заявления_на_возврат.md`.

## Использование

Установите зависимость:

```text
python -m pip install -r requirements.txt
```

Настройте пути в `config.ini`, затем запустите:

```text
python refund_application_generator.py "путь_к_файлу.json"
```

Скрипт принимает JSON в UTF-8/UTF-8 BOM или Windows-1251, проверяет данные,
заполняет xhtml2pdf-совместимый HTML-шаблон, сохраняет PDF и технический журнал
в настроенные папки, после чего открывает документ системной программой Windows.
Браузер и Microsoft Word не требуются. Каталог кириллических шрифтов задаётся
обязательным параметром `fonts_dir` в секции `[paths]` файла `config.ini`;
относительный путь отсчитывается от каталога конфигурации.

Длинная строка получателя выравнивается с отступом влево. Если после названия
организации в кавычках указано ФИО и вся строка не помещается, ФИО целиком
переносится на следующую строку после закрывающей кавычки.

## Tasks

- [x] REF-001 Реализовать формирование заявления на возврат в PDF
  Создать Windows CLI-скрипт по требованиям ТЗ: проверить JSON и config.ini,
  безопасно заполнить HTML-шаблон, рассчитать сумму, сформировать PDF через
  Playwright/Chromium, сохранить журнал и автоматически открыть готовый документ.

  **Implemented:**
  - Добавлен Windows CLI для проверки JSON и конфигурации с понятными ошибками
  - Реализовано безопасное заполнение HTML, расчёт суммы и формирование A4 PDF через Chromium
  - Поддержаны UTF-8/UTF-8 BOM и Windows-1251, уникальные имена PDF и технические журналы
  - Готовый PDF автоматически открывается назначенной системной программой

- [x] REF-002 Исправить выравнивание и перенос получателя заявления
  Сдвинуть строку получателя влево на ширину двух символов.
  Если получатель с названием организации в кавычках не помещается,
  переносить ФИО на новую строку после закрывающей кавычки.

  **Implemented:**
  - Строка получателя сдвинута влево на ширину двух символов
  - После закрывающей кавычки добавлена условная точка переноса
  - ФИО сохраняется целиком на одной строке, обычные пояснения продолжают переноситься свободно

- [x] REF-003 Перевести генератор PDF на xhtml2pdf
  Заменить Playwright/Chromium на xhtml2pdf согласно обновлённому ТЗ.
  Подключить новый HTML-шаблон и локальные DejaVu Serif TTF-шрифты.
  Обновить конфигурацию, зависимости, README и тесты.
  Сохранить существующую бизнес-логику, логирование и открытие готового PDF.

  **Implemented:**
  - PDF формируется библиотекой xhtml2pdf без браузера и внешнего конвертера
  - Подключены новый HTML-шаблон и локальные обычный/жирный DejaVu Serif
  - Добавлена безопасная загрузка только локальных ресурсов и удаление неполного PDF
  - Обновлены конфигурация, зависимости, README и реальные интеграционные тесты

- [x] REF-004 Настраивать каталог шрифтов через config.ini
  Добавить обязательный параметр `fonts_dir` в секцию `[paths]`.
  Относительный путь разрешать относительно расположения `config.ini`.
  Использовать настроенный каталог при проверке и загрузке TTF-шрифтов.
  Обновить README и тесты.

  **Implemented:**
  - Добавлен обязательный параметр `fonts_dir` с поддержкой относительных и абсолютных путей
  - TTF-шрифты проверяются и загружаются из настроенного каталога независимо от расположения шаблона
  - Сохранён запрет сетевых ресурсов и выхода за разрешённые каталоги
  - Обновлены пример конфигурации, README и интеграционные тесты PDF
