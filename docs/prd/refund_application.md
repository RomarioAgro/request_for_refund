# Формирование заявления на возврат

Автоматическое создание заявления на возврат в PDF по данным JSON согласно
`ТЗ_формирование_заявления_на_возврат.md`.

## Использование

Установите зависимость и Chromium один раз:

```text
python -m pip install -r requirements.txt
python -m playwright install chromium
```

Настройте пути в `config.ini`, затем запустите:

```text
python refund_application_generator.py "путь_к_файлу.json"
```

Скрипт принимает JSON в UTF-8/UTF-8 BOM или Windows-1251, проверяет данные,
заполняет HTML-шаблон, сохраняет PDF и технический журнал в настроенные папки,
после чего открывает документ системной программой Windows.

## Tasks

- [x] REF-001 Реализовать формирование заявления на возврат в PDF
  Создать Windows CLI-скрипт по требованиям ТЗ: проверить JSON и config.ini,
  безопасно заполнить HTML-шаблон, рассчитать сумму, сформировать PDF через
  Playwright/Chromium, сохранить журнал и автоматически открыть готовый документ.

  **Implemented:**
  - Добавлен Windows CLI для проверки JSON и конфигурации с понятными ошибками
  - Реализовано безопасное заполнение HTML, расчёт суммы и формирование A4 PDF через Chromium
  - Поддержаны UTF-8/UTF-8 BOM и Windows-1251, уникальные имена PDF и технические журналы
  - Готовый PDF автоматически открывается назначенной системной программой

- [ ] REF-002 Исправить выравнивание и перенос получателя заявления
  Сдвинуть строку получателя влево на ширину двух символов.
  Если получатель с названием организации в кавычках не помещается,
  переносить ФИО на новую строку после закрывающей кавычки.
